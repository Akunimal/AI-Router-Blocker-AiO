name: Build and Release Binaries

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Tag name to publish when release publishing is enabled"
        required: false
        default: "v1.3.2"
      release_name:
        description: "Release name to publish when release publishing is enabled"
        required: false
        default: "v1.3.2 - Product Name Cleanup"
      publish_release:
        description: "Upload build outputs to the GitHub Release"
        required: false
        default: false
        type: boolean

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: Build with PyInstaller (Windows)
        if: runner.os == 'Windows'
        run: >
          pyinstaller --onefile --windowed --uac-admin
          --add-data "translations.json;."
          --add-data "icon.ico;."
          --add-data "icon_green.ico;."
          --add-data "icon_red.ico;."
          --name "CodeGate"
          --clean ai_blocker.py

      - name: Build with PyInstaller (Unix)
        if: runner.os != 'Windows'
        run: >
          pyinstaller --onefile --windowed
          --add-data "translations.json:."
          --add-data "icon.ico:."
          --add-data "icon_green.ico:."
          --add-data "icon_red.ico:."
          --name "CodeGate"
          --clean ai_blocker.py

      - name: Rename release assets
        shell: bash
        run: |
          cd dist
          if [ "$RUNNER_OS" = "Windows" ]; then
            mv "CodeGate.exe" "CodeGate-Windows.exe"
          elif [ "$RUNNER_OS" = "macOS" ]; then
            if [ -f "CodeGate.app/Contents/MacOS/CodeGate" ]; then
              cp "CodeGate.app/Contents/MacOS/CodeGate" "CodeGate-macOS"
            fi
            if [ -d "CodeGate.app" ]; then
              zip -r "CodeGate-macOS.app.zip" "CodeGate.app"
            fi
          else
            mv "CodeGate" "CodeGate-Linux"
          fi

      - name: Generate SHA256 checksums
        shell: bash
        run: |
          cd dist
          for f in CodeGate-*; do
            sha256sum "$f" > "$f.sha256"
          done

      - name: Upload CI artifacts
        uses: actions/upload-artifact@v7
        with:
          name: CodeGate-${{ runner.os }}
          path: dist/CodeGate-*
          if-no-files-found: error

      - name: Upload GitHub Release assets
        if: github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && inputs.publish_release == true)
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.event.release.tag_name || inputs.tag_name }}
          name: ${{ github.event.release.name || inputs.release_name }}
          body_path: RELEASE_NOTES.md
          files: dist/CodeGate-*
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
